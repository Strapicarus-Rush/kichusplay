#ifndef MENU_H
#define MENU_H

// Declaration of the renderImage function
void renderImage(unsigned char *data, int width, int height, int windowWidth, int windowHeight);

// Declaration of the renderMenu function
void renderMenu(int windowWidth, int windowHeight);


#endif // MENU_H